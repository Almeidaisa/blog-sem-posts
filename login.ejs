<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            background-color: #fce4ec; /* Rosa claro */
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        .login-container {
            background-color: #fff; /* Fundo branco */
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            width: 300px;
            text-align: center;
        } 

        header {
            background-color: #e91e63;
            padding: 20px 0;
            text-align: center;
            color: #fff;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 100%;
            position: fixed;
            top: 0;
            z-index: 1000;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .login-container h2 {
            color: #e91e63; /* Rosa mais escuro */
        }

        .login-container input {
            width: 100%;
            padding: 10px;
            margin: 8px 0;
            box-sizing: border-box;
            border: 1px solid #e91e63; /* Rosa mais escuro */
            border-radius: 4px;
        }

        .login-container button {
            background-color: #e91e63; /* Rosa mais escuro */
            color: #fff; /* Texto branco */
            padding: 10px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            width: 100%;
        }
    
        
        .login-container button:hover {
            background-color: #d81b60; /* Rosa mais escuro (hover) */
        }
    </style>
</head>
<body>
<div>
    <div class="left-nav">
                  
        <a href="home.html">sair</a>
      
    </div>
</div>
<div class="login-container">
    <h2>Login</h2>
    <form action="/login" method="post">
        Gmail<input type="text" name="email" placeholder="Username" required>
        Senha<input type="password" name="password" placeholder="Password" required>
        <button type="submit">Login</button>
    </form>
</div>
<header>
    <div class="brand">Isabella Almeida</div>
    <div class="navigation">
        <button onclick="redirectToHome()">Home</button>
        <button onclick="redirectToContact()">Contato</button>
        <button onclick="redirectToAbout()">Sobre</button>
    </div>
</header>

</body>
</html>